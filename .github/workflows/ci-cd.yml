name: Flask CI/CD Pipeline

on:
  push:
    branches:
      - main  # ✅ Trigger when anyone pushes to the main branch

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    steps:
      # Step 1 – Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2 – Set up Python
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # Step 3 – Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Assuming you also need pytest itself for running tests later
          pip install pytest

      # Step 4 – Run unit tests
      - name: Run pytest
        run: |
          export PYTHONPATH=main
          pytest tests

      # Step 5 – Build Docker image
      - name: Build Docker image
        run: |
          docker build -t flask-app .

      # Note: The original file ended here. You would typically add
      # a final deployment step (e.g., pushing the image to a registry
      # or deploying to a cloud service) after the build step.